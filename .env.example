# Create a key at https://console.typesafe.ai/keys, copy this file to .env.
# Run experiments with: node --env-file=.env experiments/<name>/run.ts
TYPESAFE_API_KEY=

# Only for experiment 05 (side-by-side with an LLM). Use a dedicated, spend-capped key.
ANTHROPIC_API_KEY=
